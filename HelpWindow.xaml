<Window x:Class="WpfApp1.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Справка" Height="500" Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        KeyDown="Window_KeyDown"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
            <Setter Property="Padding" Value="0,2,0,2"/>
        </Style>

        <Style x:Key="ListItemStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="20,3,0,3"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

       
        <DataTemplate x:Key="SectionTemplate">
            <StackPanel Margin="0,0,0,10">
                <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="{Binding Header}"/>
                <ItemsControl ItemsSource="{Binding Items}" 
                              Margin="0,0,0,5"/>
                <Separator Margin="0,5,0,0" Background="#CCCCCC" Height="1"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" 
                   Text="Реализованные функции" 
                   FontSize="22" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15" 
                   Grid.Row="0"/>

        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent"
                      BorderThickness="0">
            <ItemsControl x:Name="ContentControl" 
                          ItemTemplate="{StaticResource SectionTemplate}"
                          Margin="0,0,0,5"/>
        </ScrollViewer>
    </Grid>
</Window>