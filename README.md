# Лабораторная работа 5. Построение AST и проверка контекстно-зависимых условий

## Название и цель работы
**Название:**  Построение AST и проверка контекстно-зависимых условий

**Цель:** Изучить назначение и принципы работы семантического анализатора в структуре компилятора. Освоить методы построения абстрактного синтаксического дерева (AST) и проверки контекстно-зависимых условий (семантических правил) для заданной синтаксической конструкции.
## Сведения об авторе
- **Студент:** Авдюков Богдан
- **Группа:** АП-327
  
## Постановка задачи
Развить ранее созданный синтаксический анализатор (парсер) до семантического: построить абстрактное синтаксическое дерево (AST) и реализовать проверку контекстно-зависимых условий в соответствии с индивидуальным вариантом курсовой работы.

## **Требования к программе:**

В соответствии с вариантом курсовой работы необходимо:

1. Построить абстрактное синтаксическое дерево (AST) на основе распознанной синтаксической конструкции. AST должно отражать:
    
    - Тип узла (объявление константы, функции, переменной, оператор и т.д.).
        
    - Дочерние узлы (компоненты конструкции).
        
    - Атрибуты узлов (имя, тип, значение, модификаторы).
        
2. Реализовать проверку контекстно-зависимых условий (семантических правил):
    
    - Правило 1 (уникальность идентификаторов): Проверить, что имя не было объявлено ранее в той же области видимости.
        
    - Правило 2 (совместимость типов): Проверить, что тип инициализирующего значения соответствует объявленному типу (для констант/переменных).
        
    - Правило 3 (допустимые значения): Проверить, что значение находится в допустимых пределах (для числовых типов).
        
    - Правило 4 (использование идентификаторов): Проверить, что используемые идентификаторы были объявлены ранее (для выражений).
        
3. Визуализировать результат:
    
    - Вывести AST в читаемом текстовом формате (с отступами, в JSON, в виде дерева).
        
    - Вывести сообщения о семантических ошибках с указанием позиции в коде.
## **Входные и выходные данные:**

- Вход: строка кода из области редактирования (например, const val number: Int = 10;).
    
- Выход:
    
    - AST в текстовом представлении.
        
    - Сообщения о семантических ошибках (если есть) с указанием позиции.
        
    - Количество ошибок (в отдельной строке).
## Вариант задания
Вариант номер 105: цикл for на языке Python 
### Примеры корректных входных строк для варианта
1. `for i in range(5) : print(i) ;`
2. `for x in range(10) : print(x) ;`
3.  `for k in range(0) : print(k) ;`
## Перечень реализованных проверок
1. Использование необъявленных идентификаторов:
ParseFactor для TOKEN_ID вызывает Lookup и выдаёт ошибку, если переменная не найдена
Пример:
<img width="1869" height="685" alt="image" src="https://github.com/user-attachments/assets/8f5e6ebf-899b-4741-a905-38b4e00bedf1" />
Идентификатор idx не объявлен и отображается как семантическая ошибка 
2. Совместимость типов:
Технически в моём языке существует только int, но механизм есть проверка на выход за рамки int
<img width="1872" height="857" alt="image" src="https://github.com/user-attachments/assets/64119297-079c-4335-a3f5-a51d6a2b043f" />
3. Уникальность идентификаторов
В текущей грамматике (один цикл for на программу, без других объявлений) нет способа создать конфликт уникальности в пределах одной области видимости.
4. Допустимые значения
Фактически отсутствует проверка диапазона чисел (range с отрицательным значением, переполнение, допустимые пределы).
## Структура AST:

Для текста:
for i in range (10):  
    print (idx);
В программе выводится:
<img width="685" height="235" alt="image" src="https://github.com/user-attachments/assets/d3eb728d-6909-4945-a02d-4d4cc992f83f" />

## Формат вывода AST

- Дерево выводится в текстовом виде
- Используются символы `├──`, `└──`
- Вложенность отражает структуру программы
- Каждый узел содержит атрибуты

## AST/CST

<img width="1221" height="451" alt="image" src="https://github.com/user-attachments/assets/3b8bd82e-399f-46d3-8359-6c00e7a717ab" />

`ProgramNode` — корневой узел программы
`ForNode` — узел цикла, атрибут `variable` хранит имя переменной
`RangeNode` — узел представления `range(...)`
`IntLiteralNode` — узел целочисленного литерала, атрибут `value`
`PrintNode` — узел оператора вывода
`IdentifierNode` — узел переменной, атрибут `name`

## Тестовые примеры:

<img width="1874" height="874" alt="image" src="https://github.com/user-attachments/assets/03ab5d70-bc84-4213-b3e9-a29b1a3e1bff" />
Использование необъявленного идентификатора. Спорный момент, было решено оставить.

<img width="2104" height="860" alt="image" src="https://github.com/user-attachments/assets/b9bb1e8e-89e9-4021-a500-396f827b5008" />
Выход за допустимые пределы int

<img width="2111" height="836" alt="image" src="https://github.com/user-attachments/assets/850b5972-ba92-4833-87d3-877af76ed580" />
Если допустить синтаксическую ошибку, семантический разбор не начинается
<img width="888" height="107" alt="image" src="https://github.com/user-attachments/assets/0e5696c5-a603-4b5c-86f8-cfc00ac536a1" />
