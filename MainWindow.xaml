<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="GUI для языкового процессора" Height="727" Width="1085"
        AllowDrop="True"
        PreviewDragOver="Window_PreviewDragOver"
        Drop="Window_Drop">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="217*"/>
            <ColumnDefinition Width="361*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Name="mainMenu" Height="20" VerticalAlignment="Center" Grid.ColumnSpan="3">
            <MenuItem Header="Файл">
                <MenuItem Header="Создать" Click="new_file_Click"/>
                <MenuItem Header="Открыть" Click="open_file_Click"/>
                <MenuItem Header="Сохранить" Click="save_file_Click"/>
                <MenuItem Header="Сохранить как" Click="save_as_file_Click"/>
                <MenuItem Header="Выход" Click="exitApp_Click"/>
            </MenuItem>
            <MenuItem Header="Правка">
                <MenuItem Header="Назад" Click="back_Click"/>
                <MenuItem Header="Заново" Click="front_Click"/>
                <MenuItem Header="Вырезать" Click="cut_Click"/>
                <MenuItem Header="Копировать" Click="copy_Click"/>
                <MenuItem Header="Вставить" Click="paste_Click"/>
                <MenuItem Header="Удалить" Click="delete_Click"/>
                <MenuItem Header="Выделить всё" Click="selectAll_Click"/>
            </MenuItem>
            <MenuItem Header="Текст">
                <MenuItem Header="Постановка задачи"/>
                <MenuItem Header="Грамматика"/>
                <MenuItem Header="Классификация грамматики"/>
                <MenuItem Header="Метод анализа"/>
                <MenuItem Header="Тестовый пример"/>
                <MenuItem Header="Список литературы"/>
                <MenuItem Header="Исходный код программы"/>
                <MenuItem Header="Размер шрифта">
                    <MenuItem Header="8pt" Click="SetFontSize_Click" Tag="8"/>
                    <MenuItem Header="10pt" Click="SetFontSize_Click" Tag="10"/>
                    <MenuItem Header="12pt" Click="SetFontSize_Click" Tag="12" IsChecked="True"/>
                    <MenuItem Header="14pt" Click="SetFontSize_Click" Tag="14"/>
                    <MenuItem Header="16pt" Click="SetFontSize_Click" Tag="16"/>
                    <MenuItem Header="18pt" Click="SetFontSize_Click" Tag="18"/>
                    <Separator/>
                    <MenuItem Header="Другой..." Click="SetCustomFontSize_Click"/>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Пуск" Click="Start_Click"/>
            <MenuItem Header="Справка">
                <MenuItem Header="Вызов справки" Click="questions_Click"/>
                <MenuItem Header="О программе" Click="about_Click"/>
                <Separator/>
                <MenuItem Header="Язык">
                    <MenuItem Header="Русский" Click="SetRussian_Click" IsCheckable="True"/>
                    <MenuItem Header="English" Click="SetEnglish_Click" IsCheckable="True"/>
                    <MenuItem Header="Монгол хэл" Click="SetMongolian_Click" IsCheckable="True"/>
                </MenuItem>
            </MenuItem>
        </Menu>

        <ToolBarTray Grid.Row="1" Background="White" Grid.ColumnSpan="3">
            <ToolBar Band="1" BandIndex="1">
                <Button Name="create" Width="50" Height="50" Click="new_file_Click">
                    <Image Source="/resources/create.png"/>
                </Button>
                <Button Name="open" Width="50" Height="50" Click="open_file_Click">
                    <Image Source="/resources/open.png"/>
                </Button>
                <Button Name="save" Width="50" Height="50" Click="save_file_Click">
                    <Image Source="/resources/save.png"/>
                </Button>
                <Separator/>
                <Button Name="back" Width="50" Height="50" Click="back_Click">
                    <Image Source="/resources/back.png"/>
                </Button>
                <Button Name="front" Width="50" Height="50" Click="front_Click">
                    <Image Source="/resources/forward.png"/>
                </Button>
                <Button Name="copy" Width="50" Height="50" Click="copy_Click">
                    <Image Source="/resources/copy.png"/>
                </Button>
                <Button Name="paste" Width="50" Height="50" Click="paste_Click">
                    <Image Source="/resources/insert.png"/>
                </Button>
                <Separator/>
                <Button Name="run" Width="50" Height="50" Click="Start_Click">
                    <Image Source="/resources/play.png"/>
                </Button>
                <Separator/>
                <Button Name="question" Width="50" Height="50" Click="questions_Click">
                    <Image Source="/resources/question.png"/>
                </Button>
                <Button Name="about" Width="50" Height="50" Click="about_Click">
                    <Image Source="/resources/about.png"/>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <TabControl x:Name="tabControl" Grid.Row="2" Grid.ColumnSpan="3" 
                    ItemsSource="{Binding Tabs, RelativeSource={RelativeSource AncestorType=Window}}"
                    SelectedItem="{Binding SelectedTab, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay}"
                    SelectionChanged="TabControl_SelectionChanged"
                    VirtualizingStackPanel.IsVirtualizing="False">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock Text="{Binding FileName}" VerticalAlignment="Center"/>
                        <Button Content="" 
                                Background="Red"
                                DockPanel.Dock="Right" 
                                Margin="5,0,5,0"
                                Width="5" Height="5"
                                Command="{Binding CloseCommand}"/>
                    </DockPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <avalonEdit:TextEditor 
                        Document="{Binding Document}"
                        VerticalScrollBarVisibility="Visible"
                        SyntaxHighlighting="C#"
                        FontFamily="Consolas"
                        FontSize="12"
                        ShowLineNumbers="True"
                        LineNumbersForeground="Gray"
                        HorizontalScrollBarVisibility="Auto"
                        Loaded="TextEditor_Loaded"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <DockPanel Grid.Row="3" Margin="5,2" VerticalAlignment="Center" Grid.ColumnSpan="3">
            <ComboBox x:Name="cmbSearchType" Width="180" Margin="5,0" DockPanel.Dock="Left">
                <ComboBoxItem Content="Слова не на 's'/'S'" Tag="WordsNotEndingWithS" IsSelected="True"/>
                <ComboBoxItem Content="Комментарии Python" Tag="PythonComments"/>
                <ComboBoxItem Content="Время (ЧЧ:ММ:СС)" Tag="Time"/>
            </ComboBox>
            <Button x:Name="btnSearch" Content="Найти" Width="80" Margin="5,0" Click="btnSearch_Click" DockPanel.Dock="Left"/>
            <TextBlock x:Name="txtSearchCount" Text="Найдено: 0" VerticalAlignment="Center" Margin="10,0" DockPanel.Dock="Left"/>
        </DockPanel>

        <TabControl x:Name="bottomTabControl" Grid.Row="4" Grid.ColumnSpan="3" Margin="0,5,0,0">
            <TabItem Header="Лексемы">
                <DataGrid x:Name="DataGrid1" IsReadOnly="True"
                          AutoGenerateColumns="False" HorizontalGridLinesBrush="DarkGray"
                          RowBackground="LightGray" AlternatingRowBackground="White"
                          SelectionChanged="DataGrid1_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Условный код" Binding="{Binding Code}" Width="auto"/>
                        <DataGridTextColumn Header="Тип лексемы" Binding="{Binding Type}" Width="*"/>
                        <DataGridTextColumn Header="Лексема" Binding="{Binding Value}" Width="*"/>
                        <DataGridTextColumn Header="Местоположение" Binding="{Binding Location}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Синтаксические ошибки">
                <DataGrid x:Name="SyntaxErrorGrid" IsReadOnly="True"
                  AutoGenerateColumns="False" HorizontalGridLinesBrush="DarkGray"
                  RowBackground="LightCoral" AlternatingRowBackground="MistyRose"
                  SelectionChanged="SyntaxErrorGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Неверный фрагмент" Binding="{Binding ErrorFragment}" Width="*"/>
                        <DataGridTextColumn Header="Местоположение" Binding="{Binding Location}" Width="*"/>
                        <DataGridTextColumn Header="Описание ошибки" Binding="{Binding Description}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Семантические ошибки">
                <DataGrid x:Name="SemanticErrorGrid" IsReadOnly="True"
                      AutoGenerateColumns="False" HorizontalGridLinesBrush="DarkGray"
                      RowBackground="LightYellow" AlternatingRowBackground="LemonChiffon"
                      SelectionChanged="SemanticErrorGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Неверный фрагмент" Binding="{Binding ErrorFragment}" Width="*"/>
                        <DataGridTextColumn Header="Местоположение" Binding="{Binding Location}" Width="*"/>
                        <DataGridTextColumn Header="Описание ошибки" Binding="{Binding Description}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="AST">
                <TextBox x:Name="AstOutput"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     Margin="5"/>
            </TabItem>
            <TabItem Header="IR до оптимизаций">
                <TextBox x:Name="IrBeforeBox" IsReadOnly="True" 
             VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" 
             AcceptsReturn="True" TextWrapping="NoWrap" Margin="5"/>
            </TabItem>
            <TabItem Header="IR после оптимизаций">
                <TextBox x:Name="IrAfterBox" IsReadOnly="True" 
             VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" 
             AcceptsReturn="True" TextWrapping="NoWrap" Margin="5"/>
            </TabItem>
            <TabItem Header="Результаты поиска">
                <DataGrid x:Name="SearchDataGrid" IsReadOnly="True"
                          AutoGenerateColumns="False" HorizontalGridLinesBrush="DarkGray"
                          RowBackground="LightBlue" AlternatingRowBackground="AliceBlue"
                          SelectionChanged="SearchDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Найденная подстрока" Binding="{Binding FoundText}" Width="*"/>
                        <DataGridTextColumn Header="Позиция" Binding="{Binding Position}" Width="*"/>
                        <DataGridTextColumn Header="Длина" Binding="{Binding Length}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
        </TabControl>

        <StatusBar Grid.Row="5" Grid.ColumnSpan="2" Background="LightGray" 
                   Padding="5,2" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="statusText" Text="Готово" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="cursorPosition" Text="Стр: 1  Стб: 1" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="fileInfo" Text="Символов: 0" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="errorCountText" Text="Ошибок: 0" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="fontSizeStatus" Text="Размер: 12pt" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>